version: 1.0
runtime: nodejs14
build:
  commands:
    pre-build:
      - n 14.18.3
    build:      
      - pip install -r chatbot.js
run: 
  command: start chatbot.js
  network:
    port: 8080
    env: APP_PORT
